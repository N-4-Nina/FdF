#include <stdio.h>

float max(float val1, float val2)
{
    if (val1 > val2)
        return (val1);
    else
        return (val2);
}

float min(float val1, float val2)
{
    if (val1 < val2)
        return (val1);
    else
        return (val2);
}